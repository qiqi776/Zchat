# 🌸 Zchat - Web IM Client

**Zchat** 是一个基于 **Vue 3 + TypeScript + Go** 构建的即时通讯Web 客户端,使用 **Tailwind CSS** 构建轻量、高性能的界面

## ✨ 特性

- **现代化技术栈**：Vue 3 Composition API、Script Setup 和 TypeScript
- **轻量 UI**：移除 Element Plus，使用 Tailwind CSS 实现原子化样式，加载速度极快
- **状态管理**：使用 **Pinia** 替代 Vuex，提供更好的类型推断和模块化管理
- **实时通讯**：基于 WebSocket 实现消息的实时收发
- **用户认证**：登录、注册（含表单验证、JWT 存储）
- **聊天系统**：会话列表、实时消息气泡、自动滚动
- **通讯录**：折叠式分组列表（联系人、我的群聊、加入的群聊）
- **群组管理**：支持创建群聊
- **个人中心**：个人资料展示与管理

## 🛠 技术栈

### 前端

- **框架**: [Vue.js 3](https://vuejs.org/)
- **语言**: [TypeScript](https://www.typescriptlang.org/)
- **构建工具**: [Vite](https://vitejs.dev/)
- **样式库**: [Tailwind CSS](https://tailwindcss.com/)
- **状态管理**: [Pinia](https://pinia.vuejs.org/)
- **路由**: [Vue Router 4](https://router.vuejs.org/)

### 后端

- **语言**: Golang
- **Web 框架**: Gin
- **ORM**: Gorm
- **数据库**: MySQL
- **协议**: HTTP (Restful API) + WebSocket

## 🚀 快速开始

### 1. 环境准备

- Node.js > 16.0
- Go > 1.18 (后端)
- MySQL (后端)

### 2. 前端启动

```bash
# 进入前端目录
cd web/Zchat-web

# 安装依赖
npm install

# 启动开发服务器
npm run dev

```

访问地址通常为: `http://localhost:5173`

### 3. 后端启动

```bash
# 进入后端目录
cd cmd/Zchat

# 下载依赖
go mod tidy

# 启动服务
go run main.go

```

后端默认端口通常为: `8080`

### 4. 接口代理配置

为避免跨域问题，请确保 `vite.config.ts` 配置正确：

```typescript
server: {
  proxy: {
    '/api': {
      target: 'http://localhost:8080', // 你的 Go 后端地址
      changeOrigin: true,
      rewrite: (path) => path.replace(/^\/api/, '')
    }
  }
}

```
